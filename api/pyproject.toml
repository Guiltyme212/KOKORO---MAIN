[project]
name = "kokoro-api"
version = "0.0.1"
description = "Kokoro meditation generation backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "httpx>=0.27",
    "anthropic>=0.40",
    "structlog>=24.4",
    "python-multipart>=0.0.27",
    "mutagen>=1.47.0",
    "PyJWT[crypto]>=2.10",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.7",
    "mypy>=1.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kokoro_api"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = ["integration: tests that hit real upstream APIs"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "ASYNC", "S", "RUF"]
ignore = ["S101"]  # allow assert in tests

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S", "RUF001", "E501"]
# Prompt files are mostly natural-language prose with intentional Cyrillic.
"src/kokoro_api/prompt/system.py" = ["E501", "RUF001", "RUF002", "RUF003"]
"src/kokoro_api/prompt/user.py" = ["E501", "RUF001", "RUF002", "RUF003"]
# Encoding fix must run before structlog import on Windows.
"src/kokoro_api/main.py" = ["E402"]
# Long human-readable error message.
"src/kokoro_api/pipeline/validate_lyrics.py" = ["E501"]

[tool.mypy]
strict = true
python_version = "3.12"
