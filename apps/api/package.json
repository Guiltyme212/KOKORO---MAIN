{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn kokoro_api.main:app --reload --port 8787",
    "lint": "uv run ruff check && uv run mypy src",
    "check-types": "uv run mypy src",
    "test": "uv run pytest"
  }
}
