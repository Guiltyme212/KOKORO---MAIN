{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "(test -f .env || cp .env.example .env) && uv sync --all-extras && uv run uvicorn kokoro_api.main:app --reload --port 8787",
    "build": "uv sync --frozen",
    "lint": "uv run ruff check && uv run mypy src",
    "check-types": "uv run mypy src",
    "test": "uv run pytest"
  }
}
